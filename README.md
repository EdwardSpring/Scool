This Project was created to service the school ui repo(built with angular). 
This service is built using: 
-Java Springboot
-Hibernate
-Spring data JPA
-In memory database(h2) but also contains configuration for mySQL
